"""Lightning Liquidity Advisor — a read-only, recommend-only tool that reads a
node's state and gives plain-language liquidity recommendations.

See ../SPEC.md for the full design. This package currently implements
milestone M0 (scaffold + node snapshot).
"""

__version__ = "0.0.1"
